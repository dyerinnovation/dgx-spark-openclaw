FROM debian:bookworm-slim

# OpenClaw runs on ARM64 (DGX Spark is Grace Blackwell)
# No GPU needed — Claude runs via API

RUN apt-get update && apt-get install -y \
    curl \
    ca-certificates \
    git \
    python3 \
    python3-pip \
    && rm -rf /var/lib/apt/lists/*

# Install OpenClaw
# Install OpenClaw — skip interactive setup (configured at runtime via soul.md)
RUN curl -fsSL https://openclaw.ai/install.sh | bash -s -- --no-setup || \
    (which openclaw && echo "OpenClaw installed, setup skipped" || exit 1)

# Create openclaw user
RUN useradd -m -s /bin/bash openclaw
USER openclaw
WORKDIR /home/openclaw

EXPOSE 18789

# Gateway mode — listens for WebSocket connections from Slack/integrations
CMD ["openclaw", "gateway", "--port", "18789"]
